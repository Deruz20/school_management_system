import { ParentPortalClient } from "./parent-portal-client";

export const metadata = {
  title: "Parent Portal | NOVA School Management ERP",
  description: "Comprehensive parent and guardian portal for academic progress, fee statements, boarding welfare, and digital exeat consents."
};

export default function ParentPortalPage() {
  return <ParentPortalClient />;
}
